import { TypeList } from "docs-ui"

# VariantAvailabilityResult

 **VariantAvailabilityResult**: `object`

The computed inventory availability for variants in a given sales channel.
The object's keys are the variant IDs.
