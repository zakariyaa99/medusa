import { TypeList } from "docs-ui"

# DeleteOrderExchangeItemActionWorkflowInput

The details of the outbound item to remove from an exchange.

## Properties

<TypeList types={[{"name":"exchange_id","type":"`string`","description":"The ID of the exchange to remove the item from.","optional":false,"defaultValue":"","expandable":false,"children":[]},{"name":"action_id","type":"`string`","description":"The ID of the action associated with the item to remove.\nEvery item has an `actions` property, whose value is an array of actions.\nYou can find the action with the name `ITEM_ADD` using the item's `action` property,\nand use the value of the action's `id` property.","optional":false,"defaultValue":"","expandable":false,"children":[]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" sectionTitle="DeleteOrderExchangeItemActionWorkflowInput"/>
