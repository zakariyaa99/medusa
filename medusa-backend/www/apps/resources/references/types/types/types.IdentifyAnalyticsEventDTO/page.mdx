import { TypeList } from "docs-ui"

# IdentifyAnalyticsEventDTO

 **IdentifyAnalyticsEventDTO**: [IdentifyActorDTO](../../interfaces/types.IdentifyActorDTO/page.mdx) \| [IdentifyGroupDTO](../../interfaces/types.IdentifyGroupDTO/page.mdx)

Either the `actor_id` or the `group` property must be set.
